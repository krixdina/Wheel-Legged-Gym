# 串口通信协议

本文档用于约定 NUC 上位机与下位机之间的串口通信数据帧格式。基本约定

- 字节序：所有 `float` 均使用小端序。
- 帧结构：`[SOF][payload][CRC8][EOF]`。
- 帧头 `SOF`：`0xFF`。
- 帧尾 `EOF`：`0x0D`。
- NUC 发送的动作量是策略网络输出，不是最终关节力矩；下位机需要将动作转换为虚拟腿目标，再完成 VMC/PD 控制。

## NUC 发送到下位机

NUC 下发 6 维策略动作，payload 格式为 `<6f`，共 `24` 字节；启用 CRC8 后整帧共 `27` 字节。

| 位数 | data | 数据类型 | 真实物理含义 |
|---:|---|---|---|
| 0 | `SOF = 0xFF` | `uint8` | 帧头，用于标识一帧数据开始 |
| 1-4 | `L_theta` | `float` | 左虚拟腿摆角目标动作 |
| 5-8 | `L_l0` | `float` | 左虚拟腿长目标动作 |
| 9-12 | `L_wheel` | `float` | 左轮速度目标动作 |
| 13-16 | `R_theta` | `float` | 右虚拟腿摆角目标动作 |
| 17-20 | `R_l0` | `float` | 右虚拟腿长目标动作 |
| 21-24 | `R_wheel` | `float` | 右轮速度目标动作；下位机转换为 |
| 25 | `CRC8` | `uint8` | CRC8 校验字节，校验范围为第 `0-24` 字节 |
| 26 | `EOF = 0x0D` | `uint8` | 帧尾，用于标识一帧数据结束 |

## 下位机发送到 NUC

下位机上传 21 维原始状态量，payload 格式为 `<21f`，共 `84` 字节；启用 CRC8 后整帧共 `87` 字节。

| 位数 | data | 数据类型 | 真实物理含义 |
|---:|---|---|---|
| 0 | `SOF = 0xFF` | `uint8` | 帧头，用于标识一帧数据开始 |
| 1-4 | `base_ang_vel_x` | `float` | 机体坐标系 x 轴角速度，单位 `rad/s`，约对应 roll 角速度 |
| 5-8 | `base_ang_vel_y` | `float` | 机体坐标系 y 轴角速度，单位 `rad/s`，约对应 pitch 角速度 |
| 9-12 | `base_ang_vel_z` | `float` | 机体坐标系 z 轴角速度，单位 `rad/s`，约对应 yaw 角速度 |
| 13-16 | `projected_gravity_x` | `float` | 重力单位向量在机体系 x 轴的投影 |
| 17-20 | `projected_gravity_y` | `float` | 重力单位向量在机体系 y 轴的投影 |
| 21-24 | `projected_gravity_z` | `float` | 重力单位向量在机体系 z 轴的投影，直立时约为 `-1` |
| 25-28 | `command_vx` | `float` | 目标前进速度，单位 `m/s` |
| 29-32 | `command_wz` | `float` | 目标偏航角速度，单位 `rad/s` |
| 33-36 | `command_height` | `float` | 目标机身高度，单位 `m` |
| 37-40 | `theta0_left` | `float` | 左虚拟腿相对机体竖直方向的摆角，单位 `rad` |
| 41-44 | `theta0_right` | `float` | 右虚拟腿相对机体竖直方向的摆角，单位 `rad` |
| 45-48 | `theta0_dot_left` | `float` | 左虚拟腿摆角速度，单位 `rad/s` |
| 49-52 | `theta0_dot_right` | `float` | 右虚拟腿摆角速度，单位 `rad/s` |
| 53-56 | `L0_left` | `float` | 左虚拟腿长，单位 `m` |
| 57-60 | `L0_right` | `float` | 右虚拟腿长，单位 `m` |
| 61-64 | `L0_dot_left` | `float` | 左虚拟腿长变化率，单位 `m/s` |
| 65-68 | `L0_dot_right` | `float` | 右虚拟腿长变化率，单位 `m/s` |
| 69-72 | `left_wheel_pos` | `float` | 左轮关节角位置，单位 `rad` |
| 73-76 | `right_wheel_pos` | `float` | 右轮关节角位置，单位 `rad` |
| 77-80 | `left_wheel_vel` | `float` | 左轮关节角速度，单位 `rad/s` |
| 81-84 | `right_wheel_vel` | `float` | 右轮关节角速度，单位 `rad/s` |
| 85 | `CRC8` | `uint8` | CRC8 校验字节，校验范围为第 `0-84` 字节 |
| 86 | `EOF = 0x0D` | `uint8` | 帧尾，用于标识一帧数据结束 |

