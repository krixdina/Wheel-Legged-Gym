# 27维状态量与6维动作量说明

本文档说明 `wheel_legged_vmc_fyt` / `sim2sim` 当前策略使用的观测量和动作量含义。

策略当前帧普通观测为 27 维。历史编码器会把最近 5 帧普通观测拼接成 135 维 `obs_history`，编码出 3 维 latent；actor 实际输入为当前 27 维观测与 3 维 latent 的拼接，输出 6 维动作。

## 关节顺序

原始 6 个自由度顺序为：

```text
[left_thigh, left_leg, left_wheel, right_thigh, right_leg, right_wheel]
```

FYT 构型映射到 VMC 虚拟腿坐标系时使用：

```text
theta1 = [-q_left_thigh,  q_right_thigh]
theta2 = [-q_left_leg + pi/2,  q_right_leg + pi/2]
```

其中 `theta0` 表示虚拟腿相对机体竖直方向的摆角，`L0` 表示髋部到轮端/足端的等效腿长。

## 27维普通观测

| 维度 | 名称 | 含义 | 缩放 |
|---:|---|---|---:|
| 0 | `base_ang_vel_x` | 机体坐标系 x 轴角速度，约对应 roll 角速度 | `0.25` |
| 1 | `base_ang_vel_y` | 机体坐标系 y 轴角速度，约对应 pitch 角速度 | `0.25` |
| 2 | `base_ang_vel_z` | 机体坐标系 z 轴角速度，约对应 yaw 角速度 | `0.25` |
| 3 | `projected_gravity_x` | 重力单位向量在机体系 x 轴的投影 | `1.0` |
| 4 | `projected_gravity_y` | 重力单位向量在机体系 y 轴的投影 | `1.0` |
| 5 | `projected_gravity_z` | 重力单位向量在机体系 z 轴的投影，直立时约为 `-1` | `1.0` |
| 6 | `command_vx` | 目标前进速度，单位 m/s | `2.0` |
| 7 | `command_wz` | 目标偏航角速度，单位 rad/s。取值范围$[-\pi,\pi]$ | `0.25` |
| 8 | `command_height` | 目标机身高度，单位 m | `5.0` |
| 9 | `theta0_left` | 左虚拟腿摆角 | `1.0` |
| 10 | `theta0_right` | 右虚拟腿摆角 | `1.0` |
| 11 | `theta0_dot_left` | 左虚拟腿摆角速度 | `0.05` |
| 12 | `theta0_dot_right` | 右虚拟腿摆角速度 | `0.05` |
| 13 | `L0_left` | 左虚拟腿长 | `4.5` |
| 14 | `L0_right` | 右虚拟腿长 | `4.5` |
| 15 | `L0_dot_left` | 左虚拟腿长变化率 | `0.25` |
| 16 | `L0_dot_right` | 右虚拟腿长变化率 | `0.25` |
| 17 | `left_wheel_pos` | 左轮关节角位置 | `1.0` |
| 18 | `right_wheel_pos` | 右轮关节角位置 | `1.0` |
| 19 | `left_wheel_vel` | 左轮关节角速度 | `0.05` |
| 20 | `right_wheel_vel` | 右轮关节角速度 | `0.05` |
| 21 | `last_action_0` | 上一控制步左虚拟腿摆角动作 | `1.0` |
| 22 | `last_action_1` | 上一控制步左虚拟腿长动作 | `1.0` |
| 23 | `last_action_2` | 上一控制步左轮速度动作 | `1.0` |
| 24 | `last_action_3` | 上一控制步右虚拟腿摆角动作 | `1.0` |
| 25 | `last_action_4` | 上一控制步右虚拟腿长动作 | `1.0` |
| 26 | `last_action_5` | 上一控制步右轮速度动作 | `1.0` |

观测拼接顺序对应代码中的：

```text
base_ang_vel(3)
projected_gravity(3)
commands[vx, wz, height](3)
theta0(2)
theta0_dot(2)
L0(2)
L0_dot(2)
wheel_pos(2)
wheel_vel(2)
last_actions(6)
```

## 6维动作量

| 维度 | 名称 | 含义 | 转换为物理目标 |
|---:|---|---|---|
| 0 | `L_theta` | 左虚拟腿摆角目标动作 | `theta0_ref_left = action[0] * 0.6` |
| 1 | `L_l0` | 左虚拟腿长目标动作 | `l0_ref_left = action[1] * 0.1 + 0.2` |
| 2 | `L_wheel` | 左轮速度目标动作 | `wheel_vel_ref_left = action[2] * 6.0` |
| 3 | `R_theta` | 右虚拟腿摆角目标动作 | `theta0_ref_right = action[3] * 0.6` |
| 4 | `R_l0` | 右虚拟腿长目标动作 | `l0_ref_right = action[4] * 0.1 + 0.2` |
| 5 | `R_wheel` | 右轮速度目标动作 | `wheel_vel_ref_right = action[5] * 6.0` |

动作不是直接关节力矩。底层控制先将动作转换为虚拟腿目标，再通过 VMC 和 PD 控制得到真实关节力矩。

## 底层控制映射

虚拟腿摆角、腿长和轮速控制律为：

```text
torque_leg   = kp_theta * (theta0_ref - theta0) - kd_theta * theta0_dot
force_leg    = kp_l0    * (l0_ref     - L0)     - kd_l0    * L0_dot
torque_wheel = wheel_kd * (wheel_vel_ref - wheel_vel)
```

当前参数为：

```text
kp_theta = 80.0
kd_theta = 4.0
kp_l0 = 900.0
kd_l0 = 45.0
wheel_kd = 0.25
feedforward_force = 80.0
```

FYT 构型中，VMC 输出的左右腿关节力矩映射回真实 6 自由度顺序为：

```text
[-T1_left, -T2_left, left_wheel_torque,
  T1_right, T2_right, right_wheel_torque]
```

最终力矩会被裁剪到：

```text
[30.0, 30.0, 4.5, 30.0, 30.0, 4.5]
```

## 坐标系与正负号约定

所有带方向的状态量都应在各自所在坐标系下遵循右手定则；这里的“各自所在坐标系”很重要，因为并不是所有状态量都直接在世界坐标系中解释。

机器人机体系通常按如下方式理解：

```text
+x: 机器人前方
+y: 机器人左侧
+z: 机器人上方
```

例如 `command_wz` 是目标偏航角速度，表示绕 `+z` 轴的角速度命令，单位为 `rad/s`。按照右手定则：

```text
command_wz > 0: 绕 +z 正方向旋转，机器人向左转
command_wz < 0: 绕 +z 负方向旋转，机器人向右转
command_wz = 0: 不主动转向
```

从俯视图看，`command_wz > 0` 会让机器人朝向从 `+x` 转向 `+y`，也就是逆时针方向旋转。注意 `command_wz` 是角速度命令，不是目标角度；例如 `3.14` 表示期望机器人以约 `3.14 rad/s` 的偏航角速度左转。

各类观测量的坐标系含义如下：

| 状态量 | 坐标系/约定 | 正负号含义 |
|---|---|---|
| `base_ang_vel` | `base_link` 机体系 | 绕机体 `x/y/z` 轴，按右手定则为正 |
| `projected_gravity` | 重力向量投影到 `base_link` | 不是角度，三个分量表示重力方向在机体系下的表达 |
| `commands[vx, wz, height]` | 运动命令坐标约定 | `vx > 0` 表示向前，`wz > 0` 表示左转 |
| `theta0` / `theta0_dot` | 统一后的 VMC 坐标系 | 左右虚拟腿均在同一 VMC 坐标系下解释 |
| `L0` / `L0_dot` | 统一后的 VMC 坐标系 | 等效腿长及其变化率，不是角度量 |
| `wheel_pos` / `wheel_vel` | 对应轮关节轴方向 | 按轮关节轴的右手定则累计角度/角速度 |

## VMC坐标系与腿部状态

虽然左右腿在 URDF 原始关节坐标中的正方向存在镜像关系，但策略看到的腿部状态并不是左右各自原始关节坐标下的角度。FYT 构型会先把左右腿映射到同一套 VMC 坐标系中：

```text
theta1 = [-q_left_thigh,  q_right_thigh]
theta2 = [-q_left_leg + pi/2,  q_right_leg + pi/2]
```

当前 VMC 坐标系使用右腿参考的平面约定：

```text
VMC +x: 指向 base_link 的 -x 方向
VMC +y: 指向竖直向下方向
```

在这个 VMC 平面中，`theta1` 表示第一段连杆方向角，`theta2` 表示第二段连杆相对第一段连杆的转角。随后通过正运动学计算：

```text
L0     = 髋部到轮端/足端的等效腿长
theta0 = 等效腿相对 VMC 竖直方向的摆角
```

因此 `theta0_left` 和 `theta0_right` 具有一致的几何含义。它们不是“左腿原始关节角”和“右腿原始关节角”，而是已经经过左右镜像符号统一后的虚拟腿摆角。

在训练流程中，VMC 坐标系承担了策略动作和真实关节力矩之间的中间语义层。`wheel_legged_vmc.py` 的 `step()` 流程可以概括为：

```text
1. 策略网络输出动作，动作中的腿部量表示虚拟腿目标状态：
   theta0_ref / L0_ref

2. 仿真器返回当前原始关节状态：
   q_left_thigh, q_left_leg, q_right_thigh, q_right_leg, ...

3. 环境先将原始关节状态转换到 VMC 坐标系，得到当前虚拟腿状态：
   theta0, theta0_dot, L0, L0_dot

4. 控制器比较“策略输出的目标虚拟腿状态”和“当前虚拟腿状态”，生成虚拟腿摆角力矩和腿长方向轴向力。

5. VMC 雅可比把虚拟腿上的力/力矩映射为腿部关节力矩 T1/T2。

6. 再按照左右腿镜像关系，把 VMC 力矩映射回 URDF 原始关节坐标系下的真实关节力矩。

7. 仿真器施加这些真实关节力矩，推动下一步仿真状态更新。
```

也就是说，策略网络的腿部动作不是原始关节角目标，而是 VMC 语义下的虚拟腿目标；原始关节坐标系只在读取仿真状态和最终施加关节力矩时出现。

## 轮部位置与轮速

轮关节在 URDF 中没有 `lower/upper` 位置限位，因此 `wheel_pos` 是连续累计的轮关节角位置。机器人持续前进时，轮角会不断增大；由于普通观测最终会被裁剪到 `[-100, 100]`，当累计轮角超过约 `100 rad` 后，网络看到的 `wheel_pos` 会饱和为常数。

```text
100 rad / (2*pi) ~= 15.9 圈
```

如果轮半径约为 `0.05 m`，这大约对应：

```text
15.9 * 2*pi * 0.05 ~= 5.0 m
```

因此，当连续行驶距离超过这个量级后，后续轮子转动带来的“累计轮角增加”不再能通过 `wheel_pos` 被 actor 感知。但是这不意味着机器人完全感知不到运动，因为 `wheel_vel` 仍然保留了当前轮子的转动速度信息，历史观测编码器也会根据过去一段时间的观测估计机体线速度等隐变量。

从速度跟踪和姿态稳定控制角度看，轮子的绝对角位置通常并不重要。原因是轮子是周期旋转部件，`0 rad`、`2*pi rad`、`4*pi rad` 对轮子自身姿态来说是等价的；机器人当前能否稳定、是否在前进、轮子是否打滑或是否需要增减驱动力，主要取决于当前轮速、机身姿态、虚拟腿状态和目标速度，而不是轮子已经累计转过多少圈。绝对轮角更像里程/累计位移信息，适合定位或里程计任务；对于当前策略的速度跟踪任务，它不是核心控制量。

训练中用于观测和控制的 `dof_vel` 并不直接使用 Isaac Gym 返回的速度状态，而是在每个物理步后使用有限差分计算：

```text
diff = remainder(dof_pos - last_dof_pos + pi, 2*pi) - pi
dof_vel = diff / dt
```

这样做的原因不是 Isaac Gym 返回的速度一定“错误”，而是 solver 返回的速度和控制器希望使用的速度定义不同。在有接触、关节约束、PD 驱动、摩擦和重力等复杂耦合时，仿真器很难在一个 `dt` 内一次性求出闭式精确解；它通常会构造接触不穿透、摩擦、关节约束、关节限位和驱动力等约束，然后通过多轮迭代不断修正速度和约束冲量，得到一个近似满足所有约束的下一步状态。

以轮腿机器人站在地面上为例，轮子接触地面、腿部关节受力、机身受重力、电机施加力矩，接触点又存在摩擦。如果 solver 修正了轮地接触力，这会影响腿部关节受力；腿部状态改变后又会影响机身姿态；机身姿态改变后又会反过来影响接触点。因此 solver 往往需要在一个物理步内部反复近似修正：

```text
修正接触约束 -> 修正关节约束 -> 修正摩擦约束 -> 再回头修正接触约束 -> ...
```

经过这些内部迭代后，solver 返回的关节速度更接近“这一物理步末端、约束求解器内部状态中的瞬时速度”。但策略观测和阻尼控制项通常更希望使用“两个外部采样时刻之间的平均关节速度”。例如某个关节在两个采样时刻的位置几乎一样：

```text
q(t)      ~= 0.1000 rad
q(t + dt) ~= 0.1000 rad
```

那么从控制器角度看，它在这个采样周期内的平均速度应接近 0。但在这个 `dt` 内部，solver 可能为了平衡重力、接触、PD 和摩擦做过多次小幅修正，例如：

```text
内部修正 1: 重力/接触让关节微微偏离，瞬时速度为 +0.8 rad/s
内部修正 2: 约束/阻尼把它拉回来，瞬时速度为 -0.5 rad/s
内部修正 3: PD/约束继续修正，瞬时速度为 +0.3 rad/s
内部修正 4: 最后一轮修正后，末端瞬时速度为 +0.2 rad/s
```

最终从外部采样点看，位置几乎没变：

```text
q(t + dt) - q(t) ~= 0
```

但如果读取的是 solver 最后一轮修正后的内部速度，就可能得到：

```text
dof_vel ~= +0.2 rad/s
```

于是出现“位置看起来几乎没动，但读取到的 `dof_vel` 不为 0”的现象。这个速度对 solver 的离散动力学状态来说有意义，但不一定是策略和阻尼项最想使用的速度。

位置差分速度计算的是：

```text
(q(t + dt) - q(t)) / dt
```

它更接近真实编码器在固定采样周期内得到的平均速度估计，也更适合作为策略观测和 VMC/轮速控制中的阻尼项输入。

这里还对角度差做了周期处理。旋转关节角度可能以 `[-pi, pi)` 这类周期区间表示，当关节跨过 `+pi/-pi` 边界时，数值可能从 `+3.13` 突然变成 `-3.13`。如果直接相减，会得到接近 `-2*pi` 的错误大跳变；通过：

```text
remainder(delta + pi, 2*pi) - pi
```

可以把角度差折叠回 `[-pi, pi)`，取相邻两帧之间的最短角度差，从而避免轮速或关节速度在跨越角度表示边界时突然跳变。
